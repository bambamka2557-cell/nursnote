# แผนงาน: เพิ่มรูปแบบเวร "ดึก/บ่าย (ด/บ)" ลงในตารางเวรพยาบาล (Isolated Shift Pattern Addition)

**โปรเจกต์**: LR Helper (`lr-helper__`)  
**เป้าหมาย**: เพิ่มรูปแบบเวร **"ดึก/บ่าย (ด/บ)"** (Night/Afternoon Shift) เข้าสู่ระบบตารางเวรพยาบาลห้องคลอด โดยคงความปลอดภัยสูงสุด ไม่แตะต้องฐานข้อมูล ไม่กระทบระบบเดิมใดๆ ในโปรเจกต์  
**สถานะ**: พร้อมสำหรับการตรวจสอบ (Ready for Audit by Claude)

---

## 1. บริบทและโครงสร้างเวลาการทำงานจริงของแต่ละกะ (Shift Work Schedule)

ในระบบการทำงานจริงของพยาบาล มีการขึ้นเวรดังต่อไปนี้:
- **กะเช้า (ช)**: `08:00 - 16:00 น.` (เวรเดี่ยว 8 ชม.)
- **กะบ่าย (บ)**: `16:00 - 00:00 น.` (เวรเดี่ยว 8 ชม.)
- **กะดึก (ด)**: `00:00 - 08:00 น.` (เวรเดี่ยว 8 ชม.)
- **กะบ่าย/ดึก (บ/ด)**: `16:00 - 08:00 น.` (เวรควบข้ามคืนต่อเนื่อง 16 ชม.)
- **กะดึก/บ่าย (ด/บ)**:
  - **ช่วงที่ 1 (กะดึก)**: `00:00 - 08:00 น.` (8 ชม. เริ่มตั้งแต่เที่ยงคืนถึงแปดโมงเช้า)
  - **ช่วงพักระหว่างวัน**: `08:00 - 16:00 น.` (พักผ่อนช่วงกะเช้า)
  - **ช่วงที่ 2 (กะบ่าย)**: `16:00 - 00:00 น.` (8 ชม. กลับมาขึ้นเวรบ่ายตอนเย็น)
  - **รวมในวันเดียวกัน**: นับเป็นเวร **ด/บ** ทำงานรวม 16 ชม. (ประกอบด้วย 2 กะย่อยระดับอะตอมคือ `NIGHT` และ `AFTERNOON`)

---

## 2. ขอบเขตความปลอดภัยสูงสุด (Strict Scope Isolation & Zero DB Migration)

> [!IMPORTANT]
> **ไม่มีการแก้ Database Schema และไม่ต้องทำ DB Migration ใดๆ ทั้งสิ้น**:
> 1. **Prisma Model `ShiftSchedule`**: ฟิลด์ `shifts` จัดเก็บข้อมูลเป็น JSON Array ของเวรย่อยระดับอะตอม (`SubShift[]`: `{ code: "NIGHT" | "AFTERNOON" | "MORNING", ot: boolean }`) อยู่แล้ว
> 2. **เวร "ดึก/บ่าย (ด/บ)"**: จัดเก็บลงฐานข้อมูลเป็น:
>    ```json
>    [
>      { "code": "NIGHT", "ot": false },
>      { "code": "AFTERNOON", "ot": false }
>    ]
>    ```
>    (เรียงตามลำดับเวลาขึ้นเวรจริง คือ ดึกช่วงเช้ามืด แล้วตามด้วย บ่ายช่วงเย็น)
> 3. **Server Actions** (`src/app/actions/shift.ts`, `src/app/actions/salary.ts`): ไม่ต้องแก้ไขโค้ดใดๆ เพราะฝั่ง Backend จัดการกับ SubShift ระดับอะตอมแบบ Decoupled 100%
> 4. **ไม่แตะต้องระบบส่วนอื่น**: ระบบบันทึกการคลอด (Delivery Room / Partograph), ข้อมูลมารดา-ทารก, สัญญาณชีพ, รายการยา, การแจ้งเตือน ไม่ถูกแตะต้องและไม่มีผลกระทบใดๆ

---

## 🔍 Audit Notes (Claude — เช็คเทียบโค้ดจริง ณ 2026-09-17)

> **สรุป:** แผนถูกทางและเสี่ยงต่ำ (client-side ล้วน ไม่แตะ DB/schema/server actions/reminder). **ในเชิงคำนวณ ด/บ ≡ บ/ด ทุกประการ** (atomic เดียวกัน = 16 ชม., นับ double, +1 ดึก, +1 บ่าย, เงิน = เรตดึก+เรตบ่าย) ⇒ `salary.ts`/`calculateWorkLifeBalance` **ไม่ต้องแก้เลย** ตามที่แผนบอก. สิ่งที่ต่างจริงมีแค่ **label ที่โชว์ + ข้อความ timing ใน modal**. แต่มี 3 จุดที่ต้องทำให้เป๊ะ:

### 🔴 1. ต้องแทรก `N_A` ที่ตำแหน่ง **ก่อน `OFF`** ใน `SHIFT_PATTERNS` (แผนไม่ได้ระบุตำแหน่ง)
โมดัลเรนเดอร์ด้วย `SHIFT_PATTERNS.map()` ใน `grid-cols-3` (ShiftCalendar.tsx:743-744) ⇒ **ลำดับในอาเรย์ = ลำดับปุ่มบนจอ**. ต้องเรียงเป็น:
`[ M, A, N, M_A, A_N, N_A, OFF ]` (N_A index 5, OFF ย้ายไป index 6). ถ้าเผลอ append N_A ต่อท้ายหลัง OFF เลย์เอาต์ 3+3+1 จะพัง (OFF ไปโผล่กลางแถว)

### 🔴 2. แก้ `getDayShiftDisplay` = **บังคับ ต้องมาคู่กับข้อ 1** (ไม่ใช่ optional)
ปัจจุบัน `getDayShiftDisplay` (shiftData.ts:245) fallback ด้วย **`SHIFT_PATTERNS[5]`** (hardcode index — เป็นจุด index-based เดียวในโค้ดทั้งหมด). พอแทรก N_A ก่อน OFF ⇒ index 5 กลายเป็น N_A ไม่ใช่ OFF ⇒ ป้าย fallback เพี้ยน. ต้องเปลี่ยนเป็น `SHIFT_PATTERNS.find(p => p.id === 'OFF')` **พร้อมกัน** ในคอมมิตเดียว (แผนข้อ 5.1#5 จับได้แล้ว — ย้ำว่าห้ามลืม/ห้ามแยก)

### 🟢 3. การแยก ด/บ vs บ/ด ด้วย "ลำดับ" ปลอดภัย — verify แล้ว (อย่าเผลอทำพัง)
`N_A=[NIGHT,AFTERNOON]` กับ `A_N=[AFTERNOON,NIGHT]` มี atomic เดียวกัน แยกด้วยลำดับล้วนๆ ซึ่ง**ใช้ได้จริงเพราะ**:
- `saveShift` (shift.ts) `.map()` เก็บลำดับตามที่ UI ส่ง, และ `handleSelectPattern` สร้าง sub-shifts ตามลำดับใน `subShifts` ⇒ ลำดับคงที่ตั้งแต่กดจนบันทึก
- **ไม่มี `.sort()`/`.reverse()` กับอาเรย์ `shifts` ที่ไหนเลย** (grep ยืนยัน) — **ห้ามเพิ่ม sort เข้ามาเด็ดขาด** จะทำให้ ด/บ ↔ บ/ด สลับกัน
- ข้อมูล `บ/ด` เดิมใน DB = `[AFTERNOON,NIGHT]` → ยัง map เป็น `A_N` ถูก (backward compatible)
- **คงบรรทัด fallback `includes(AFTERNOON) && includes(NIGHT) → 'A_N'` ไว้** เป็น safety net เผื่อข้อมูลลำดับเพี้ยน (default ไป บ/ด). เพราะเงิน/WLB ไม่สนลำดับอยู่แล้ว การ default ผิด label ไม่กระทบตัวเลข

### 🟡 4. เกร็ดเล็ก
- `ATOMIC_SHIFTS.AFTERNOON.timeRange` เป็น **display-only ไม่มีที่ไหน parse** (ชม.คิดจาก `count*8` hardcode) ⇒ เปลี่ยน `16:00-24:00`→`16:00-00:00` ปลอดภัย (กระทบการโชว์ บ่ายเดี่ยวด้วย ซึ่ง consistent)
- แผนอ้างระบบ Partograph / Delivery Room / สัญญาณชีพ — **ไม่มีจริงในโปรเจกต์นี้** (มีแค่ patient/order/eventLog) ไม่กระทบอะไร แค่ระบุไว้ว่า audit อิงโค้ดจริงที่เล็กกว่า
- ไม่แตะ schema ⇒ ไม่ต้อง `prisma db push` แต่ build ต้องเขียว + เช็ค Vercel Deployments เขียวตามปกติ

---

## 3. การออกแบบ UI/UX และธีมสี (Design & Symmetrical Palette)

### 3.1 ข้อมูลจำเพาะของเวร "ดึก/บ่าย (ด/บ)"
เพื่อให้เข้ากับธีมพาสเทล Hello Kitty สบายตา และแยกออกจากเวรบ่าย/ดึก (บ/ด) สีเขียวอมฟ้า (Teal) และเวรดึก (ด) สีคราม (Indigo) อย่างชัดเจน:
- **Pattern ID**: `'N_A'`
- **รหัสแสดง (Code)**: `'N/A'`
- **ชื่อย่อภาษาไทย**: `'ด/บ'`
- **ชื่อเต็ม**: `'ดึก/บ่าย (ด/บ)'`
- **เวรย่อย (subShifts)**: `['NIGHT', 'AFTERNOON']`
- **โทนสีการ์ด/ป้าย (Badge)**: ฟ้าพาสเทลสดใส
  - `badgeBg`: `'bg-sky-100/95'`
  - `badgeBorder`: `'border-sky-300'`
  - `badgeTextColor`: `'text-sky-800'`
  - `dotColor`: `'bg-sky-500'`

### 3.2 การจัดเลย์เอาต์ปุ่มใน Modal เลือกเวร (Symmetrical 3x3 Layout)
เดิมมี 6 ปุ่ม (3 คอลัมน์ x 2 แถว) เมื่อเพิ่ม `ดึก/บ่าย (ด/บ)` เข้ามา จะมีทั้งหมด 7 ตัวเลือก จัดเลย์เอาต์ให้สวยงาม สมดุล และกดง่ายบนมือถือ ดังนี้:
- **แถวที่ 1 (เวรเดี่ยว 8 ชม.)**: `[ เช้า (ช) ]` `[ บ่าย (บ) ]` `[ ดึก (ด) ]` (3 ปุ่ม)
- **แถวที่ 2 (เวรควบ 16 ชม.)**: `[ เช้า/บ่าย (ช/บ) ]` `[ บ่าย/ดึก (บ/ด) ]` `[ ดึก/บ่าย (ด/บ) ]` (3 ปุ่ม)
- **แถวที่ 3 (วันหยุดพักผ่อน)**: `[ 🌴 วันหยุด (OFF) ]` ขยายเต็มความกว้างแถว (`col-span-3`)

```
+-----------------------------------------------------------+
| [ 🌸 M : ช ]       | [ 🍇 A : บ ]       | [ 🌙 N : ด ]     |
+-----------------------------------------------------------+
| [ ☀️ M/A : ช/บ ]   | [ 🌊 A/N : บ/ด ]   | [ 🔷 N/A : ด/บ ]  |
+-----------------------------------------------------------+
|               [ 🌴 วันหยุดพักผ่อน (OFF) ]                 |
+-----------------------------------------------------------+
```

### 3.3 แถบเลือกสลับสีตัวอักษร OT ใน Modal (Red OT vs Black Normal)
เมื่อผู้ใช้แตะเลือก `ดึก/บ่าย (ด/บ)` หน้าต่าง Modal จะแสดง 2 กะย่อยตามช่วงเวลาจริง:
1. `เวรดึก (ด) 00:00 - 08:00` พร้อมปุ่มสลับ `[ ⚫ ปกติ (ตัวดำ) / 🔴 OT (ตัวแดง) ]`
2. `เวรบ่าย (บ) 16:00 - 00:00` พร้อมปุ่มสลับ `[ ⚫ ปกติ (ตัวดำ) / 🔴 OT (ตัวแดง) ]`
ช่วยให้พยาบาลกำหนดได้อิสระว่ากะใดเป็นเวรปกติ หรือกะใดเป็นเวรเสริม OT

### 3.4 แถบคำอธิบายสัญลักษณ์เวร (Shift Legend Bar)
เพิ่มจุดสีฟ้า `ด/บ` เคียงข้าง `บ/ด` ที่ด้านล่างของปฏิทิน:
- `ช (เช้า)` ชมพู
- `บ (บ่าย)` ม่วง
- `ด (ดึก)` คราม
- `ช/บ` อำพัน
- `บ/ด` เขียวอมฟ้า (Teal)
- `ด/บ` ฟ้าพาสเทล (Sky)
- ตัวดำ: ปกติ | ตัวแดง: OT

---

## 4. ผลลัพธ์ต่อการคำนวณสมดุลชีวิต (WLB) และประมาณการเงินเดือน

### 4.1 การคำนวณสมดุลชีวิตและชั่วโมงทำงาน (`calculateWorkLifeBalance`)
ฟังก์ชันคำนวณทำงานระดับ Sub-shift รายกะโดยตรงอยู่แล้ว:
- **ชั่วโมงทำงานรวม**: เพิ่ม 16 ชม. (8 ชม. + 8 ชม.) อัตโนมัติ
- **วันเวรควบ 16 ชม. (Double Shift Days)**: นับเป็น 1 วันอัตโนมัติ (`dayShifts.length >= 2`)
- **การสะสมกะดึก (Night Count)**: นับกะดึกเพิ่ม 1 กะ และนำไปคำนวณสถิติเวรดึกติดต่อกัน (Max Consecutive Nights) อัตโนมัติ
- **การสะสมกะบ่าย (Afternoon Count)**: นับกะบ่ายเพิ่ม 1 กะ อัตโนมัติ
- **สัดส่วนเวร (Ratio Bar)**: แสดงสัดส่วนกะดึกและกะบ่ายในแถบแท่งสีครบถ้วน

### 4.2 การคำนวณค่าตอบแทนเวรและเงินเดือน (`calculateDayShiftAllowance`)
- คำนวณตามอัตราเงินของเวรดึก (ปกติ/OT) รวมกับอัตราเงินของเวรบ่าย (ปกติ/OT) ตามที่พยาบาลตั้งค่าไว้ใน `NurseSalaryConfig` โดยอัตโนมัติ

---

## 5. รายละเอียดไฟล์ที่ต้องแก้ไข (Files to Modify)

### 5.1 `src/app/utils/shiftData.ts`
1. ปรับ `ATOMIC_SHIFTS.AFTERNOON.timeRange` จาก `16:00 - 24:00` เป็น `16:00 - 00:00`
2. ขยาย Type `ShiftPatternId`:
   ```typescript
   export type ShiftPatternId = 'M' | 'A' | 'N' | 'M_A' | 'A_N' | 'N_A' | 'OFF';
   ```
3. เพิ่ม Object `N_A` ลงใน `SHIFT_PATTERNS` **ที่ตำแหน่งก่อน `OFF`** (ลำดับสุดท้ายต้องเป็น `M, A, N, M_A, A_N, N_A, OFF` — ดู Audit #1):
   ```typescript
   {
     id: 'N_A',
     code: 'N/A',
     shortLabel: 'ด/บ',
     fullLabel: 'ดึก/บ่าย (ด/บ)',
     subShifts: ['NIGHT', 'AFTERNOON'],
     badgeBg: 'bg-sky-100/95',
     badgeBorder: 'border-sky-300',
     badgeTextColor: 'text-sky-800',
     dotColor: 'bg-sky-500',
   },
   ```
4. ปรับปรุงฟังก์ชัน `matchShiftPattern`:
   ```typescript
   if (codes.length === 2) {
     if (codes.includes('MORNING') && codes.includes('AFTERNOON')) return 'M_A';
     if (codes[0] === 'NIGHT' && codes[1] === 'AFTERNOON') return 'N_A';
     if (codes[0] === 'AFTERNOON' && codes[1] === 'NIGHT') return 'A_N';
     if (codes.includes('AFTERNOON') && codes.includes('NIGHT')) return 'A_N';
   }
   ```
5. **(บังคับ — คู่กับข้อ 3)** ปรับปรุง `getDayShiftDisplay` (บรรทัด 245): เปลี่ยน `SHIFT_PATTERNS[5]` → `SHIFT_PATTERNS.find(p => p.id === 'OFF')` มิฉะนั้นพอแทรก N_A ก่อน OFF ป้าย fallback จะกลายเป็น N_A (ดู Audit #2)

### 5.2 `src/app/components/ShiftCalendar.tsx`
1. เพิ่มสัญลักษณ์ `ด/บ` (จุดสีฟ้า `bg-sky-500`) ในแถบคำอธิบายสัญลักษณ์เวร (Shift Legend Bar)
2. ปรับปรุง Grid การแสดงผลปุ่มเลือกเวรใน Modal ให้ปุ่ม `OFF` ขยายเต็มความกว้าง (`col-span-3`) เพื่อความสมมาตร

### 5.3 `src/app/components/WorkLifeBalanceCard.tsx`
1. อัปเดตข้อความกล่องแสดงเวรควบ 16 ชม. จาก `ช/บ หรือ บ/ด` เป็น `ช/บ, บ/ด, ด/บ`

---

## 6. แผนการตรวจสอบและ Audit Checklist (Verification Checklist)

- [ ] **TypeScript & Build**: รัน `npm run build` ผ่าน 100% ไม่มี Type Error
- [ ] **UI Modal Check**: ปุ่ม `[ ดึก/บ่าย (ด/บ) ]` แสดงสีฟ้าพาสเทล ปุ่ม `[ วันหยุด (OFF) ]` เต็มแถวล่าง
- [ ] **Timing Check**: เมื่อเลือก `ด/บ` รายการกะย่อยแสดง `เวรดึก (00:00 - 08:00)` และ `เวรบ่าย (16:00 - 00:00)`
- [ ] **OT Color Check**: สามารถสลับตัวดำ (ปกติ) / ตัวแดง (OT) แยกกะย่อยได้อิสระ
- [ ] **Calendar Display Check**: บันทึกแล้วปฏิทินแสดงผลถูกต้อง
- [ ] **Order round-trip (จุดเสี่ยงหลัก)**: บันทึก `ด/บ` แล้ว **รีโหลด** → ยังโชว์ `ด/บ` ไม่กลายเป็น `บ/ด` (พิสูจน์ว่าลำดับ `[NIGHT,AFTERNOON]` คงอยู่ตลอด)
- [ ] **Backward-compat**: ข้อมูล `บ/ด` เดิมที่ลงไว้ก่อนหน้า → ยังแสดงเป็น `บ/ด` (A_N) ถูกต้อง ไม่เพี้ยนเป็น ด/บ
- [ ] **Grid layout**: โมดัลเรียงปุ่ม `M A N / M_A A_N N_A / OFF(เต็มแถว)` และป้าย fallback (getDayShiftDisplay) ยังเป็น OFF ถูกต้อง
- [ ] **WLB Check**: นับชั่วโมง 16 ชม., นับกะดึก 1 กะ, นับกะบ่าย 1 กะ, นับเวรควบ 1 วัน (เท่ากับ บ/ด เป๊ะ)
- [ ] **Safety Check**: `salary.ts`, `shift.ts`, schema, reminder/patient/push **ไม่แตะแม้แต่บรรทัดเดียว** (แก้เฉพาะ shiftData.ts, ShiftCalendar.tsx, WorkLifeBalanceCard.tsx)
