# แผนการพัฒนา Feature: ปฏิทินปกติ & วันหยุดราชการไทย พร้อมปุ่มกดสลับโหมด (Standard & Thai Holidays Calendar)

แผนการออกแบบระบบสำหรับเพิ่ม **ปฏิทินมาตรฐานที่แสดงวันหยุดราชการไทย วันนักขัตฤกษ์ และวันสำคัญต่างๆ** พร้อม **ปุ่มกดสลับโหมดปฏิทิน (View Toggle)** ระหว่าง "ตารางเวรพยาบาล" และ "ปฏิทินวันหยุดราชการ" โดยออกแบบเป็นโมดูลอิสระ ไม่กระทบระบบเดิม

---

## 📋 User Review & Decisions to Audit (สำหรับผู้ใช้ตรวจสอบ)

> [!IMPORTANT]
> **ประเด็นสำคัญที่ต้องการให้ออดิตและยืนยัน**:
> 1. **ตำแหน่งและสไตล์ของปุ่มสลับโหมด (View Toggle)**:
>    - ออกแบบเป็นปุ่มแคปซูล 2 แท็บ: `[ 🩺 ตารางเวร | 📅 ปฏิทินวันหยุด ]`
>    - ตำแหน่ง: วางไว้บน Header ของปฏิทิน (ข้างๆ ชื่อเดือน หรือเหนือชื่อเดือน)
>    - มีการจดจำโหมดล่าสุดใน `localStorage` เพื่อความสะดวกเมื่อกลับมาเปิดใหม่
>
> 2. **ฟังก์ชันตรวจสอบวันหยุดของพยาบาล (Holiday Cross-Check)**:
>    - ในหน้า **"ปฏิทินวันหยุด"**:
>      - ไฮไลท์วันหยุดราชการด้วยกรอบสีส้ม-ชมพูนุ่มนวล (`border-rose-300 bg-rose-50/80`)
>      - แสดงชื่อวันหยุดภาษาไทยในช่องวัน (เช่น `🎉 วันสงกรานต์`, `🌸 วันแม่`, `👑 วันปิยมหาราช`, `✨ วันปีใหม่`)
>      - **แสดงสถานะเวรของพยาบาลในวันหยุดนั้นทันที**:
>        - หากไม่มีเวร (OFF/ไม่มี row): แสดงป้ายเขียวสดใส **"🌴 ได้หยุด"**
>        - หากมีเวร: แสดงป้ายเวรที่ขึ้นจริง เช่น **"🩺 ขึ้นเวรเช้า (ช)"** — สี ดำ/แดง ตาม `shift.ot` จริงของเวรนั้น **(ห้ามเหมาว่าเป็น OT เพราะตรงวันหยุด — OT รพ.กำหนดในตาราง ไม่ผูกกับวันหยุด ดู Audit #1)**
>      - ช่วยให้พยาบาลตอบคำถามสำคัญ: *"วันหยุดยาวรอบนี้ฉันได้หยุดเที่ยวไหม หรือติดเวรวันไหนบ้าง?"*
>
> 3. **ในโหมด "ตารางเวร" (Shift View เดิม)**:
>    - ยังคงเน้นเวร เช้า/บ่าย/ดึก, สี OT ดำ/แดง, สติกเกอร์, โน้ต และการคิดเงินเดือนตามเดิม 100%
>    - เพิ่มจุดสีแดงกุหลาบจิ๋ว 🎈 หรือไอคอนวันหยุดเล็กๆ กำกับที่มุมเลขวัน หากวันนั้นตรงกับวันหยุดราชการ เพื่อให้พยาบาลสังเกตเห็นได้ทันทีโดยไม่ต้องสลับหน้า

---

## 🔍 Audit Notes (Claude — เช็คเทียบโค้ดจริง + บริบทฟีเจอร์ก่อนหน้า)

> **Scope:** ฟีเจอร์นี้เป็น **client-side ล้วน** — ไฟล์ข้อมูล static + แก้ UI ปฏิทิน (`ShiftCalendar` เป็น `'use client'` อยู่แล้ว) **ไม่มี schema/DB/server** ⇒ ไม่ต้อง `prisma db push`, ไม่มีความเสี่ยง Vercel schema-fail เงียบ, ไม่มีปัญหา timezone ฝั่ง server (จับคู่วันหยุดด้วย string `YYYY-MM-DD` บน browser ผู้ใช้ที่อยู่ Asia/Bangkok อยู่แล้ว) — ยังต้องให้ `npm run build` ผ่าน + เช็ค Vercel เขียวตามปกติ
>
> **✅ ยืนยันจากผู้ใช้ (2026-09-08):** ปฏิทินวันหยุดนี้เป็น **reference/ดูเฉยๆ ไม่ป้อนค่าเข้าการคำนวณใดๆ** (ไม่แตะเงินเดือน/OT/สถิติ WLB) — ให้เขียนเป็น guardrail ชัดๆ ว่าข้อมูลวันหยุด **ห้ามไหลเข้า `salary.ts`/`shiftData` calc เด็ดขาด** (กันอนาคตมีคน "ช่วย" wire เข้าหากัน). ผู้ใช้ทำ รพ.รัฐ แต่ยังไม่บรรจุ ⇒ **ใช้ชุดวันหยุดปฏิทินทั่วไปมาตรฐาน** (แบบที่ปฏิทินทั่วไปแสดง รวมวันแรงงาน) **ไม่ต้องกรองตามประเภทการจ้าง**

### 🔴 ต้องแก้ก่อน (correctness/semantic)
1. **"ทำงานวันหยุดราชการ" ≠ "OT" — ห้าม conflate กัน** (สำคัญสุด): ฟีเจอร์เงินเดือนที่ทำไปแล้ว ตกลงกันชัดว่า **สีแดง/OT รพ. กำหนดเป็นราย sub-shift ในตารางเวร (`shift.ot`) ไม่ได้ derive จากวันหยุดปฏิทิน** ⇒ พยาบาลขึ้นเวรวันสงกรานต์อาจเป็น "ดำ (ปกติ)" ก็ได้ ไม่ใช่ OT อัตโนมัติ. Cross-check ต้องโชว์แค่ "**ขึ้นเวรอะไร**" + สีจริงของ sub-shift นั้น (`shift.ot`) **ห้าม hardcode ป้าย "(OT วันหยุด)"** กับทุกเวรที่ตรงวันหยุด (ดูแก้ label ในหัวข้อ 2 ด้านล่าง). **และห้าม auto-set `shift.ot=true` จากวันหยุดเด็ดขาด** — 2 ระบบนี้อิสระต่อกัน
2. **ข้อมูลวันหยุดต้องมาจากแหล่งทางการ ไม่ใช่ความจำ AI**: วันหยุดจันทรคติ (มาฆบูชา/วิสาขบูชา/อาสาฬหบูชา/เข้าพรรษา), ลอยกระทง, และวันหยุดพิเศษที่ ครม. ประกาศเพิ่มรายปี — **ผิดง่ายมากถ้าเดา** ต้อง verify กับประกาศทางการ (เช่น ธปท./สำนักนายกฯ) รายปี และ **ใส่ comment อ้างอิงแหล่ง + ปีที่อัปเดตล่าสุด** ในไฟล์ข้อมูล
   - *(หมายเหตุ: ปฏิทินนี้ดูเฉยๆ ไม่กระทบเงิน ⇒ วันที่ผิดคือ "น่าเกลียด" ไม่ใช่ "อันตราย" — แต่ก็ควรถูกอยู่ดี. วันหยุดชดเชยจะทำหรือไม่ทำก็ได้ ระบุให้ชัด)*
   - **Graceful fallback:** ตารางจันทรคติ hardcode ถึงปี 2028 ⇒ ปีนอกช่วง `getThaiHoliday` ต้องคืน `null` (ไม่ crash) และ UI ควรมีข้อความเบาๆ ว่า "ข้อมูลวันหยุดถึงปี YYYY" ไม่ใช่แสดงว่าง/ผิดเงียบๆ
   - **วันเด็ก (เสาร์ที่ 2 ม.ค.) และลอยกระทง (จันทรคติ) ไม่ใช่ MM-DD คงที่** — ต้องคำนวณ/hardcode ราย­ปี ไม่ใช่เก็บเป็น date เดียว

### ✅ Resolved (ผู้ใช้ยืนยันแล้ว 2026-09-08)
3. **ประเภทการจ้าง — ไม่ต้องกรอง**: เป็นปฏิทินดูเฉยๆ ⇒ ใช้ชุดวันหยุดทั่วไปมาตรฐาน **รวมวันแรงงาน (1 พ.ค.)** ตามที่ปฏิทินทั่วไปแสดง. **ไม่ต้องทำ flag `forGov`/`forPrivate`** (ตัด `forGov` ออกจาก interface ได้เพื่อความเรียบง่าย). วันพืชมงคลจะใส่หรือไม่ก็ได้ — ถ้าใส่ต้อง hardcode รายปี (เลื่อนทุกปี)

### 🟡 ปรับให้เนียน (ไม่ใช่บั๊ก)
4. **สถิติวันหยุด/Monthly card ใช้ "เดือนปฏิทิน" ไม่ใช่รอบจ่าย 26→25** — ปฏิทินแสดงเป็นเดือนปฏิทินอยู่แล้ว (`currentYear`/`currentMonth`), วันหยุดก็อิงเดือนปฏิทิน ⇒ **อย่าเผลอเอา `getCycleRangeForMonth` (26→25) มาใช้กับ holiday stats** จะเพี้ยน
5. **นับเฉพาะ `isPublicHoliday === true`** ในสถิติ "วันหยุดราชการ" — วันสำคัญทั่วไป (วาเลนไทน์/วันครู/วันพยาบาล) ไม่ใช่วันหยุด ไม่ควรถูกนับรวมในการ์ดสรุป (แสดงแยกได้ แต่ไม่ปนตัวเลข)
6. **`ShiftCalendar.tsx` ปัจจุบัน ~675 บรรทัดแล้ว** — แนะนำแยก "Monthly Holidays Card" เป็นคอมโพเนนต์ใหม่ (เช่น `HolidaySummaryCard.tsx` แนว `SalarySummaryCard`) และวางตรรกะข้อมูลไว้ใน `thaiHolidays.ts` ทั้งหมด. ช่องปฏิทินบนมือถือแน่นมาก (`min-h-[70px]`) — ชื่อวันหยุดในช่อง (holiday view) ต้อง truncate/ฟอนต์เล็ก กันล้น
7. ทำ view toggle จำค่าใน `localStorage` — เข้ากับ pattern ปุ่ม privacy ที่มีอยู่ (`SalarySummaryCard`) ใช้ `mounted` guard กัน hydration mismatch เหมือนกัน

---

## 🏗️ สรุปโครงสร้างสถาปัตยกรรมและไฟล์ที่จะพัฒนา

```mermaid
graph TD
    A[ปุ่มสลับโหมด View Toggle] -->|เลือกโหมด| B{โหมดปัจจุบัน}
    
    B -->|🩺 ตารางเวร (Shift View)| C[Shift Calendar Mode]
    C --> C1[เวร ช / บ / ด / OFF]
    C --> C2[สีตัวอักษร OT ดำ-แดง]
    C --> C3[จุดกำกับวันหยุดราชการแบบย่อ]
    C --> C4[การ์ดสรุป Work-Life Balance & เงินเดือน]
    
    B -->|📅 ปฏิทินวันหยุด (Holiday View)| D[Thai Holiday Mode]
    D --> D1[ไฮไลท์วันหยุดราชการและวันสำคัญ]
    D --> D2[Holiday Cross-Check: แสดงว่าได้หยุด OFF หรือติดเวร]
    D --> D3[การ์ดสรุปวันหยุดประจำเดือน Monthly Holidays Card]
    
    E[ฐานข้อมูลวันหยุดไทย thaiHolidays.ts] --> C3
    E --> D1
    E --> D3
```

---

## 📁 รายละเอียดไฟล์และสิ่งที่สร้างใหม่

### 1. ฐานข้อมูลวันหยุดไทย (`src/app/utils/thaiHolidays.ts`) [สร้างใหม่]
สร้างฐานข้อมูลวันหยุดราชการไทยและวันสำคัญทางศาสนาพุทธ/สากล ครอบคลุม:
- **วันหยุดราชการคงที่ (Fixed Solar Holidays)**:
  - 01-01: วันขึ้นปีใหม่
  - 04-06: วันพระบาทสมเด็จพระพุทธยอดฟ้าจุฬาโลกมหาราชและวันที่ระลึกมหาจักรีบรมราชวงศ์ (วันจักรี)
  - 04-13: วันสงกรานต์ (วันผู้สูงอายุแห่งชาติ)
  - 04-14: วันสงกรานต์ (วันครอบครัว)
  - 04-15: วันสงกรานต์
  - 05-01: วันแรงงานแห่งชาติ
  - 05-04: วันฉัตรมงคล
  - 06-03: วันเฉลิมพระชนมพรรษาสมเด็จพระนางเจ้าฯ พระบรมราชินี
  - 07-28: วันเฉลิมพระชนมพรรษาพระบาทสมเด็จพระเจ้าอยู่หัว (ร.10)
  - 08-12: วันแม่แห่งชาติ (วันเฉลิมพระชนมพรรษาสมเด็จพระบรมราชชนนีพันปีหลวง)
  - 10-13: วันนวมินทรมหาราช (วันคล้ายวันสวรรคต ร.9)
  - 10-23: วันปิยมหาราช
  - 12-05: วันพ่อแห่งชาติ / วันชาติ (วันคล้ายวันพระบรมราชสมภพ ร.9)
  - 12-10: วันรัฐธรรมนูญ
  - 12-31: วันสิ้นปี
- **วันหยุดราชการทางศาสนาพุทธ (Lunar Buddhist Holidays - ปี 2024 - 2028+)**:
  - วันมาฆบูชา
  - วันวิสาขบูชา
  - วันอาสาฬหบูชา
  - วันเข้าพรรษา
- **วันสำคัญทั่วไป (Observances & Special Days)**:
  - วันครู (16 ม.ค.), วันเด็กแห่งชาติ (เสาร์ที่ 2 ม.ค.), วันวาเลนไทน์ (14 ก.พ.), วันสตรีสากล (8 มี.ค.), วันพยาบาลแห่งชาติ (21 ต.ค.), วันลอยกระทง, วันคริสต์มาส (25 ธ.ค.)
- **Types & Interfaces**:
  ```ts
  export interface ThaiHoliday {
    date: string; // YYYY-MM-DD
    name: string; // เช่น "วันสงกรานต์"
    isPublicHoliday: boolean; // true = วันหยุดราชการ/นักขัตฤกษ์ (นับในสถิติ), false = วันสำคัญทั่วไป (โชว์ได้ ไม่นับ)
    badgeColor?: string;
    icon?: string;
    description?: string;
  }

  export interface HolidayMonthStats {
    year: number;
    month: number;          // เดือนปฏิทิน 1-12 (ไม่ใช่รอบจ่าย 26→25 — ดู Audit #4)
    publicHolidays: ThaiHoliday[];
    totalPublicHolidays: number;   // นับเฉพาะ isPublicHoliday === true
    offOnHolidays: number;         // วันหยุดที่ "ได้หยุดจริง" (ไม่มีเวร)
    workedOnHolidays: number;      // วันหยุดที่ "ต้องขึ้นเวร" (มี sub-shift อย่างน้อย 1)
  }
  ```
- **Helper Functions**:
  - `getThaiHoliday(dateStr: string): ThaiHoliday | null` — คืน `null` ถ้าไม่ใช่วันหยุด **หรือปีอยู่นอกช่วงข้อมูลจันทรคติ** (ไม่ throw)
  - `getHolidaysForMonth(year: number, month: number): ThaiHoliday[]` — เดือนปฏิทิน
  - `getHolidayStatsForMonth(shifts: DayShiftData[], year: number, month: number): HolidayMonthStats` — จับคู่วันหยุด (isPublicHoliday) กับ `shifts` ด้วย string `date` ตรงๆ; "ขึ้นเวร" = วันนั้นมี `shifts.length > 0` (ไม่ตัดสินว่า OT — ดู Audit #1)

> **⚠️ Audit — ไฟล์ข้อมูลนี้ (ต้องอ่านก่อนกรอกวันที่):**
> - **อ้างอิงแหล่งทางการเท่านั้น** (ประกาศวันหยุด ธปท./สำนักนายกฯ รายปี) — ใส่ comment หัวไฟล์ระบุแหล่ง + ปีที่ครอบคลุม + วันที่อัปเดตล่าสุด. **ห้าม generate วันจันทรคติจากความจำ** (มาฆ/วิสาข/อาสาฬห/เข้าพรรษา/ลอยกระทง เลื่อนทุกปี ผิดง่าย)
> - **วันหยุดชดเชย** (วันหยุดตรงเสาร์-อาทิตย์ → หยุดชดเชยวันทำการถัดไป) v1 จะทำหรือไม่ทำก็ได้ แต่ต้อง**ระบุให้ชัดว่าเลือกอะไร** (ถ้าไม่ทำ = อาจนับวันหยุดคลาดจากจริง — เป็น known limitation ที่ยอมรับได้ ไม่ใช่ซ่อนไว้)
> - **วันเด็ก = เสาร์ที่ 2 ม.ค., ลอยกระทง = ขึ้น 15 ค่ำเดือน 12** ⇒ ไม่ใช่ MM-DD คงที่ ต้องคำนวณ/hardcode รายปี

---

### 2. ปรับปรุงคอมโพเนนต์ปฏิทิน (`src/app/components/ShiftCalendar.tsx`) [แก้ไข]
- **เพิ่ม State โหมดปฏิทิน**:
  ```tsx
  const [calendarMode, setCalendarMode] = useState<'shift' | 'holiday'>('shift');
  ```
- **ปุ่ม Toggle บน Header ปฏิทิน**:
  - ปุ่ม 2 แท็บพร้อมไอคอน:
    - `🩺 ตารางเวร`
    - `📅 วันหยุดราชการ`
- **การเรนเดอร์ในช่องวันแต่ละโหมด**:
  - **โหมดตารางเวร (Shift View)**:
    - แสดงป้ายเวร (ช, บ, ด, ช/บ, บ/ด, OFF) พร้อมสี OT ดำ/แดง ตามเดิม
    - ถ้าวันนั้นเป็นวันหยุดราชการ: มีไอคอนริบบิ้น 🎈 หรือจุดสีชมพูกุหลาบเล็กๆ ข้างเลขวัน
  - **โหมดวันหยุด (Holiday View)**:
    - ไฮไลท์กรอบช่องวันหยุดราชการด้วยสีส้ม-ชมพูอ่อน
    - แสดงชื่อวันหยุดภาษาไทยในช่องวัน เช่น `🎉 วันสงกรานต์`
    - แสดงป้าย Cross-Check เวรของพยาบาล (ดึงจาก `shifts` ของวันนั้น):
      - ถ้าไม่มีเวร: `🌴 ได้หยุด`
      - ถ้ามีเวร: `🩺 ขึ้นเวร [ช/บ/ด/ช-บ/บ-ด]` โดยสีตัวอักษร ดำ/แดง = `shift.ot` จริงของ sub-shift นั้น (reuse `getDayShiftDisplay` จาก `shiftData.ts`) — **ห้ามพ่วงคำว่า "OT วันหยุด" ให้ทุกเวรอัตโนมัติ**
- **การ์ดสรุปวันหยุดประจำเดือน (Monthly Holidays Card)** — แนะนำแยกเป็นคอมโพเนนต์ `HolidaySummaryCard.tsx` (Audit #6):
  - แสดงเฉพาะเมื่ออยู่ในโหมด "ปฏิทินวันหยุด"
  - รายการวันหยุดราชการ (`isPublicHoliday`) ทั้งหมดของ**เดือนปฏิทิน**นั้น (ไม่ใช่รอบ 26→25)
  - แถบสรุป 3 สถิติ (จาก `HolidayMonthStats`):
    1. 🎌 วันหยุดราชการทั้งหมดในเดือน: `totalPublicHolidays` วัน
    2. 🌴 วันหยุดที่คุณได้พักผ่อนจริง (ไม่มีเวร): `offOnHolidays` วัน
    3. 🩺 วันหยุดที่คุณติดเวร (มีเวร): `workedOnHolidays` วัน *(ไม่พ่วง "OT" — ดู Audit #1)*

---

## 🧪 แผนการทดสอบ (Verification Plan)

1. **ตรวจความถูกต้องของข้อมูล (สำคัญสุดของฟีเจอร์นี้)**:
   - **เทียบวันหยุดจันทรคติ (มาฆ/วิสาข/อาสาฬห/เข้าพรรษา/ลอยกระทง) กับประกาศทางการรายปี** ปีต่อปี — ไม่ใช่เชื่อค่าที่ AI ใส่มา (จุดผิดง่ายที่สุด)
   - วันหยุดสุริยคติคงที่: เม.ย. (จักรี 6 + สงกรานต์ 13-15), ส.ค. (วันแม่ 12), ต.ค. (นวมินทร์ 13 + ปิยมหาราช 23), ธ.ค. (พ่อ 5 + รัฐธรรมนูญ 10 + สิ้นปี 31)
   - **ปีนอกช่วงข้อมูล** (เช่น 2030): `getThaiHoliday` คืน `null` ไม่ crash + UI ขึ้นข้อความ "ข้อมูลถึงปี YYYY"
   - `getHolidayStatsForMonth` นับ**เฉพาะ `isPublicHoliday`** และใช้**เดือนปฏิทิน** (ไม่ใช่ 26→25)
2. **Unit test — OT อิสระจากวันหยุด (กัน regression ของฟีเจอร์เงินเดือน)**:
   - ลงเวรวันสงกรานต์แบบ **`ot=false` (ดำ)** → Holiday view ต้องโชว์ "ขึ้นเวร (ตัวดำ ปกติ)" **ไม่ใช่ "OT"** และยอดเงินไม่เปลี่ยนเป็น OT
3. **Next.js Production Build**: `npm run build` ผ่าน 100% (Exit 0) — ฟีเจอร์นี้ไม่แตะ schema จึงไม่ต้อง db push แต่ยังต้อง **เช็ค Vercel Deployments เขียว** หลัง deploy
4. **Manual บนหน้าเว็บจริง**:
   - กดสลับโหมด `[ 🩺 ตารางเวร | 📅 ปฏิทินวันหยุด ]` แล้ว **รีโหลด** → โหมดล่าสุดยังอยู่ (localStorage + `mounted` guard ไม่ hydration-mismatch)
   - โหมดตารางเวร: วันหยุดมีจุด/ไอคอนจิ๋วที่มุมเลขวัน โดยเวร/สี/เงิน ทำงานเดิม 100%
   - โหมดวันหยุด: ไฮไลท์ + ชื่อวันหยุด (truncate ไม่ล้นช่องบนมือถือ `min-h-[70px]`) + Cross-Check "ได้หยุด"/"ขึ้นเวร" ถูกต้อง + ตัวเลขการ์ดสรุปตรง
